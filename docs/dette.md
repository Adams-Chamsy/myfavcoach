# Dette technique

Dettes créées volontairement pendant l'initialisation du projet (lot L0), à traiter au lot
indiqué.

| Dette | Raison | Lot de traitement |
|---|---|---|
| `npm test` tourne avec `--passWithNoTests` | `src/test/` est vide à l'initialisation ; sans ce drapeau, `npm run verif` échouerait tant qu'aucun test n'existe. À retirer dès les premiers tests écrits. | L0, dès la première primitive testée |
| `app.json` ne référence aucune icône ni écran de démarrage | Aucun asset de marque n'existe encore dans le dépôt ; en inventer un serait une donnée de contenu non fournie. | L0, quand les assets de marque arrivent |
| Node local (22.12.0) sous la version minimale de React Native (`^22.13.0`) | Contrainte de l'environnement de développement au moment de l'initialisation, pas du code. `.nvmrc` fixe la version cible. | Avant le premier `npm start` réel |
| `jest` figé en 29.x (et `@types/jest` avec) | `jest-expo` embarque des outils Jest 29.x en interne (`babel-jest`, `jest-environment-jsdom`, `@jest/globals` en `^29`) ; `jest` 30 casse au runtime (`clearMocksOnScope is not a function`). À réévaluer quand `jest-expo` supportera Jest 30. | L12 |
| `previsualiserEtat` sur `Bouton` et `BoutonIcone` (force l'affichage de l'état pressé ou focus) | Ces deux états sont pilotés en interne par les événements `Pressable`, sans prop exposée ; la galerie (L0-00, section 4) doit pourtant les montrer côte à côte de façon statique, sans interaction réelle. Réservé au fichier `app/_galerie.tsx`, jamais utilisé par un écran produit. | Aucun — mécanisme définitif, à surveiller seulement si `Bouton` change de gestion d'état |
| Hauteur du bloc média de la forme `detail` de `Squelette` (200 pt) en constante locale `HAUTEUR_MEDIA_DETAIL` | Aucun ratio ni token de taille ne couvre le portrait/hero d'un écran de détail générique — distinct du plein cadre de profil coach (`taille.pleinCadre`, 400 pt), qui a un usage précis et documenté. | Quand une taille dédiée sera ajoutée à `design/tokens.json` |
| Taille du logo de démarrage (64 pt) en constante locale `TAILLE_LOGO` (`app/index.tsx`) | docs/ecrans/L0-04-demarrage.md fixe « le M en Instrument Serif, 64 pt » ; `texte.display` (44), le plus proche, n'est pas 64 et n'a pas vocation à l'être (chiffre clé d'écran, pas logo). | Quand un token dédié (ex. `taille.logoDemarrage`) sera ajouté à `design/tokens.json` |
| Critères nécessitant un appareil ou un navigateur réel jamais vérifiés (L0-00 critères 1 et 5 ; L0-01 critères 5 et 6 ; L0-02 critère 5 ; L0-04 critère 1 ; L1-01 critère 7) | Cet environnement de développement n'a ni simulateur ni navigateur : démarrage à froid, bascule sombre à l'œil, mise à l'échelle du texte à 200 %, zones sûres d'un iPhone à encoche ou d'un Android à barre gestuelle n'ont jamais été observés pour de vrai. Les mécanismes existent et sont testés unitairement (insets par prop, `minHeight` non figée, `allowFontScaling` par défaut de React Native), mais l'écran ne l'a jamais été. Pour L1-01 (`app/(public)/index.tsx`) : vérifié que l'accroche n'est jamais tronquée (`numberOfLines` absent) et que le panneau d'actions n'a pas de hauteur fixe, mais pas que le texte tient réellement sur trois lignes sans chevauchement visuel à 200 % — ça demande un moteur de rendu réel. Les métriques fixes de `SafeAreaProvider` utilisées par les tests (`src/test/accessibilite.test.tsx`, `app/(public)/index.test.tsx` : haut 59, bas 34) correspondent à un vrai appareil, vérifié — iPhone 15/14 Pro, insets confirmés par recherche externe — mais ne couvrent qu'un cas parmi d'autres, pas le pire cas : une barre de navigation Android à 3 boutons (par opposition aux gestes) peut atteindre 48 dp, plus que les 34 utilisés ici. Plus fondamental : **aucun test actuel, ni le corpus générique ni celui de L1-01, ne mesure la hauteur totale du contenu rendu contre la hauteur de l'écran** — un écran sans défilement (comme L1-01) pourrait déborder réellement sur un appareil plus petit ou à 200 %, sans qu'aucun test ne le voie. Choisir des métriques différentes n'aurait rien changé à ce trou : il est structurel, pas lié aux valeurs retenues. | Avant publication, sur appareil physique — envisager, pour L1-01 spécifiquement, un test de secours si le contenu dépasse jamais une hauteur d'écran plausible |
| `npm run test:a11y` ne rend pas `app/_layout.tsx`, `app/index.tsx`, ni les `_layout.tsx` de `(client)`/`(coach)` | Nécessiteraient de simuler `expo-font`/`expo-splash-screen`/`expo-secure-store` et un contexte de navigation `Tabs` réel. Leurs seules couleurs propres (`marque.primaire` et `texte.secondaire` sur `fond.canevas` ; `BarreNavigation`, déjà rendue via la galerie) sont déjà mesurées ailleurs dans le corpus. Voir le commentaire en tête de `src/test/accessibilite.test.tsx`. | Si ces fichiers gagnent une couleur qui n'apparaît nulle part ailleurs dans le corpus |
| Aucun test ne simule `useColorScheme() === 'dark'` pour prouver à l'exécution que le thème sombre système est ignoré | La preuve actuelle est une lecture de code (aucun appel à `useColorScheme` dans `src/theme/fournisseur.tsx`) et un test qui vérifie seulement la valeur par défaut (`src/theme/fournisseur.test.tsx`), pas un test qui force le système en sombre et vérifie que rien ne change. | Quand le mode sombre sera réellement conçu (hors jalon 1, docs/perimetre.md §3) |
| `src/fixtures/demonstration.ts` : offres, séances, avis et messages incomplets | Seule l'offre de Yannick Berthaud a une description et des avantages (la seule que le dossier de design détaille) ; le titre de l'offre de Thomas Kieffer est repris de son programme, faute de carte d'offre dans les maquettes lues ; les dates de message restent des libellés relatifs (« hier », « 12 août »), aucune maquette ne fixant de date de référence absolue pour un `envoyeLe` ISO 8601 réel. Le fichier documente chaque absence à l'endroit concerné. | Quand chaque lot construira l'écran réel correspondant (L2 offres, L6 programmes, L8 messagerie) |
| Le test d'un fichier `_layout.tsx` ne peut pas s'appeler `_layout.test.tsx` — `app/_layout-racine.test.tsx` (tiret) pour `app/_layout.tsx` | expo-router traite tout nom commençant par le segment exact « _layout » avant le premier point comme une variante de layout (même mécanisme que `_layout.ios.tsx`) ; `_layout.test.tsx` s'enregistre donc comme un second layout de la même route et fait planter le serveur de développement au démarrage (« The layouts ... conflict on the route »), y compris en production. `resolver.blockList` (metro.config.js) ne protège pas de ce cas : la construction de l'arbre de routes lève avant que blockList n'intervienne. Concerne aussi, le jour où ils gagnent un test : `app/(client)/_layout.tsx`, `app/(coach)/_layout.tsx`, `app/(coach)/creer/_layout.tsx`. | Aucun — contrainte durable d'expo-router, à respecter pour tout nouveau `_layout.tsx` testé |
| Correction #11 de `docs/design-system.md` (bouton primaire pressé illisible en thème sombre) trouvée à l'œil, pas par les tests | `npm run test:a11y` tournait depuis P0.14 mais ne rendait qu'en `themeForce="clair"` : le défaut, propre au thème sombre, lui était invisible. Corrigé (la suite rend désormais la galerie sous les deux thèmes), mais toute nouvelle surface ajoutée à `app/_galerie.tsx` doit rester exercée dans les deux — sinon le même angle mort revient sans qu'aucun test ne le signale. | Aucun — vigilance à chaque ajout dans la galerie |
| Purge des comptes non vérifiés à 30 jours non implémentée (`docs/domaine.md` §4.1 : `en_attente_verification --(30 jours sans confirmation)--> supprime`) | Nécessite une tâche planifiée côté serveur (Supabase Edge Function + cron), pas encore écrite à ce lot. | L11, avec la conformité |
| Protection contre les mots de passe divulgués (Have I Been Pwned) non activée | Réglage Supabase Auth réservé au plan Pro et au-delà — indisponible sur le plan gratuit du projet de développement, confirmé dans la documentation Supabase (`supabase.com/docs/guides/auth/password-security`). À activer sur les deux projets dès que le passage en Pro (`docs/backend.md` §1) aura lieu. | L4, avec le passage en Pro |
| Session bornée dans le temps (« time-box user sessions ») non activée | Même contrainte que ci-dessus : réglage Supabase Auth réservé au plan Pro et au-delà (`supabase.com/docs/guides/auth/sessions`), indisponible sur le plan gratuit. Valeur cible une fois disponible : 30 jours (`docs/backend.md` §3). | L4, avec le passage en Pro |
| **En local**, `npm run test:rls` (banc RLS) tourne contre le projet Supabase de **développement** distant, partagé avec l'usage manuel du développeur | La pile Supabase locale (`supabase start`) ne tourne pas de façon fiable sur cette machine (8 Go de mémoire, disque saturé) : en local, `npm run verif` cible donc le projet distant. Conséquence assumée : le banc peut entrer en collision avec un usage manuel simultané du même projet (isolé par un préfixe d'adresse et un identifiant unique par exécution, `docs/prompts/L1.md` P1.5). **En CI, ce n'est plus le cas** : depuis P1.15, `.github/workflows/banc-rls.yml` monte une pile Supabase **éphémère** sur le runner (`supabase start` + `supabase db reset`) et y lance le banc — la garde de `rls.banc.ts` autorise l'hôte `localhost` en plus du projet distant. | Le distant partagé reste la cible locale tant que cette machine ne peut pas faire tourner `supabase start` ; un projet Supabase distant **dédié aux tests** (pour découpler aussi l'usage manuel) reste souhaitable — L4, avec le passage en Pro |
| La politique `profils_coach_insert_espace_coach` (0002_politiques.sql) ne protège rien aujourd'hui — ce n'est pas seulement une limite du banc | Vérifié, pas supposé : son `WITH CHECK` exige d'être déjà en espace coach ; `basculer_profil` exige à son tour qu'un profil coach existe déjà pour autoriser ce passage (0002_politiques.sql). Personne ne peut donc satisfaire sa condition par ce chemin — et comme aucune autre politique INSERT n'existe sur cette table, la retirer aujourd'hui ne changerait RIEN : RLS refuse déjà tout INSERT direct par défaut, avec ou sans elle (cycle rouge/vert de P1.5 : 21/21 verts dans les deux cas, y compris avec un scénario qui tente explicitement l'INSERT illégitime). Elle est fonctionnellement inerte, pas seulement hors de portée du banc. Elle pourrait le rester même après L2 : le commentaire de 0002_politiques.sql fixe déjà que `creer_profil_coach` devra être `SECURITY DEFINER` — ce qui, comme `basculer_profil` le fait pour `comptes.profil_actif`, la contournera plutôt que de s'appuyer dessus. Elle ne redeviendrait utile que si un jour une insertion directe authentifiée (hors `creer_profil_coach`) était réellement voulue — rien de tel n'est écrit aujourd'hui. Tranché franchement : pas de formulation rassurante possible ici. | À revoir au lot L2 : si un chemin d'insertion directe existe vraiment, ajouter le cas légitime au banc ; sinon, documenter explicitement qu'elle n'est qu'un garde-fou de complétude (une politique explicite par opération, jamais un défaut implicite) |
| `src/fonctionnalites/navigation/barre-onglets.test.tsx` réputé instable | A échoué une fois par dépassement de délai le 1er septembre 2026, sans qu'aucun commit ne touche le fichier ni son composant (`git log` : dernier commit sur les deux, L0). Repasse depuis, de façon reproductible : 1,2 à 2,0 s sur cinq exécutions isolées consécutives, aucune variance suspecte. Cause non identifiée ; hypothèse de contention (machine à 8 Go de RAM, disque saturé ce jour-là) plausible mais non prouvée — pas une explication rassurante choisie faute de mieux. Fichier relu une fois à la recherche d'un geste non attendu (le défaut qui a produit les faux verts de L0) : aucun trouvé, chaque `fireEvent.press` est `await`, chaque assertion suit un rendu déjà stabilisé. | À rouvrir à la première récidive, pas avant — pas de correctif tant que la cause n'est pas reproduite |
| `src/services/auth/supabase.ts`, traduction de l'erreur `age_insuffisant` — NON VÉRIFIÉE | Signalé à P1.5 (`src/test/rls.banc.ts`, commentaire sur le déclencheur des 18 ans) : on ne sait pas si GoTrue relaie le message du déclencheur (`0001_creer_identite.sql`, "reserve aux majeurs") ou seulement un code générique (`unexpected_failure`, code documenté par `@supabase/auth-js` pour toute erreur serveur imprévue). Hypothèse la plus probable écrite dans le code, marquée comme telle : chercher la trace du déclencheur (`/majeur/i`) dans le message, sinon retomber sur `'serveur'` plutôt que de risquer l'inverse. Si l'hypothèse est fausse, l'écran L1-02 affichera le message générique au lieu du message dédié « My fav Coach est réservé aux majeurs. » — pas de faille, juste moins précis. | À lever quand l'écran L1-02 sera testé sur un vrai appareil, avec une vraie tentative d'inscription mineure contre le projet de développement |
| Fenêtre de désaccord possible entre l'écran L1-02 (âge calculé en heure locale de l'appareil) et le déclencheur des 18 ans (calcule en UTC côté serveur) | Vérifié en direct sur le projet de développement : `SHOW timezone` → `UTC`. Le déclencheur utilise `age(new.date_naissance)`, fondé sur `current_date` du serveur, donc UTC — jamais Europe/Paris. Paris étant toujours en avance sur UTC, une personne qui a son 18ᵉ anniversaire aujourd'hui peut se voir refusée par le serveur (422 `age_insuffisant`) alors que l'écran, calculant en local, la considère déjà majeure — pendant la fenêtre entre minuit heure de Paris et minuit UTC (1 à 2 h selon l'heure d'été). Le sens inverse (base accepte, écran refuse) n'arrive jamais pour un utilisateur en France. Pas une faille — le serveur reste la règle, toujours plus prudent, jamais plus permissif — mais un refus surprenant possible, une fois dans la vie de chaque utilisateur pile ce jour-là. Le port (`ResultatAuth`) permet déjà cette réponse quel que soit le contrôle client. | À traiter à P1.8 (écran L1-02) : ne pas supposer qu'un contrôle client passé rend `age_insuffisant` impossible côté serveur — prévoir ce cas dans les tests de l'écran |
| `app/(public)/index.tsx` (L1-01) : quatre gestes rendus mais non câblés | Assumé, pas oublié — chacun documenté à l'endroit précis dans le code : (1) « Continuer avec Apple » — `onPress` vide : aucune méthode Apple dans `src/services/auth/port.ts` (P1.6 ne couvre que l'e-mail/mot de passe), `expo-apple-authentication` n'est pas une dépendance du dépôt, et le parcours n'est de toute façon pas testable dans Expo Go (fiche L1-01, "En attente de build de développement"). (2) Les deux liens de mention légale (CGU, confidentialité) — `onPress` vide : aucune URL n'existe avant la rédaction des textes (lot L11) ; aucune URL n'a été inventée. (3) L'intention `viaCoach` — portée en paramètre de requête sur l'URL d'inscription (`?viaCoach=1`) : mécanisme provisoire, jamais confirmé contre ce que L1-08 (activation espace coach, qui la consommera après création du compte) attendra réellement. | Apple : quand Sign In with Apple sera réellement construit, hors jalon 1 tant que non planifié. Liens légaux : lot L11. `viaCoach` : à confirmer en écrivant L1-08 |
| Expéditeur du courriel de confirmation : celui par défaut de Supabase, pas `bonjour@myfavcoach.fr` | docs/ecrans/L1-02/L1-03 demandent `bonjour@myfavcoach.fr` comme expéditeur. Ce n'est pas un réglage de gabarit (`supabase/templates/confirmation.html`, déjà en français, tutoiement) : c'est un réglage SMTP (`[auth.email.smtp]`), qui exige un vrai fournisseur (SendGrid, Postmark…) avec de vrais identifiants — aucun n'est choisi, aucun n'a été inventé. Sans lui, Supabase envoie depuis son propre service par défaut, adresse non personnalisable. | Quand un fournisseur SMTP sera choisi : ajouter `[auth.email.smtp]` à `supabase/config.toml` avec ses identifiants (jamais commités), puis `supabase config push` |
| `src/services/auth/supabase.ts`, `etablirSessionDepuisLien` — flux PKCE supposé, NON VÉRIFIÉ | Les DEUX liens profonds qui l'appellent — courriel de vérification (myfavcoach://auth/rappel) ET, depuis P1.9, réinitialisation de mot de passe (myfavcoach://auth/mot-de-passe) — doivent porter `?code=...` (flux PKCE, le défaut de `@supabase/supabase-js` v2 quand `flowType` n'est pas fixé — vérifié dans `src/services/supabase/client.ts` : il ne l'est pas) et être échangés via `exchangeCodeForSession(code)`. Si le projet utilise en réalité le flux implicite (jetons dans le FRAGMENT `#access_token=...&refresh_token=...`), ce code échoue silencieusement, sans jamais établir de session — inatteignable depuis ici (aucun accès à la boîte de réception, `bonjour@myfavcoach.fr` n'a pas de SMTP configuré, voir l'entrée juste au-dessus). C'est exactement le critère 2 de docs/ecrans/L1-03-verification-email.md, qui prévoit déjà "deux essais manuels sur appareil" — pas une omission de test, une limite déjà anticipée par la fiche ; P1.9 (L1-04) partage la même limite pour le lien de réinitialisation, sans critère dédié qui le prévoie explicitement dans sa propre fiche. | À lever au premier essai manuel sur appareil (critère 2 de L1-03) : si le lien porte des jetons en fragment plutôt qu'un `code`, ajouter le chemin `setSession({ access_token, refresh_token })` à `etablirSessionDepuisLien` — corrige les deux consommateurs à la fois |
| `src/services/supabase/stockage-securise.ts` n'a aucune persistance sur web (`Platform.OS === 'web'` → `getItem` renvoie toujours `null`, `setItem`/`removeItem` ne font rien) | Trouvé à P1.8 : `npm run verif:serveur` (qui fait tourner un vrai `expo start --web`, seul moyen de faire tourner un vrai serveur sur cette machine sans simulateur — voir l'entrée juste en dessous) plantait à l'ouverture avec `TypeError: ExpoSecureStore.default.getValueWithKeyAsync is not a function`. Vérifié dans le code du paquet : `node_modules/expo-secure-store/src/ExpoSecureStore.web.ts` exporte un objet vide — aucune implémentation web n'existe dans la version installée (57.0.1), pas une erreur "indisponible" propre. Le web n'est de toute façon jamais une cible du produit (CLAUDE.md §1 : iOS + Android uniquement) : une session qui ne survit pas au rechargement de page y est un comportement honnête, pas un bug — jamais AsyncStorage/localStorage en repli, qui donnerait une vraie persistance à une plateforme jamais conçue pour porter un jeton de session. | Aucun — comportement définitif tant que le web n'est pas une cible réelle, à revoir seulement si `docs/perimetre.md` §3 change |
| `app/(public)/inscription.tsx` (L1-02) jamais essayé bout en bout sur un appareil réel | Trouvé à P1.8 en repérant que `@react-native-community/datetimepicker` n'a lui non plus aucune implémentation web (`node_modules/@react-native-community/datetimepicker/src/datetimepicker.js`, le repli sans suffixe de plateforme, rend `null` — voir `CLAUDE.md` §6, l'angle mort structurel que ça a révélé). Ce qui reste NON VÉRIFIÉ, spécifiquement : le vrai geste de confirmation sur chaque plateforme — le dialogue Android (`display="default"`, boutons OK/Annuler natifs) et le sélecteur "spinner" iOS avec son bouton "Valider la date" ajouté à ce lot n'ont été exercés que par des `fireEvent(..., 'valueChange', ...)` synthétiques (`app/(public)/inscription.test.tsx`), jamais par un vrai doigt sur un vrai écran ; et le critère 7 de la fiche ("à 200 %, les trois champs et le bouton restent atteignables par défilement, le pied reste fixe"), jamais observé avec un moteur de rendu réel — même limite structurelle que l'entrée L0-00/L0-01/L0-02/L0-04/L1-01 plus haut. | Sur appareil physique (iOS et Android), avant publication — pris en charge par l'utilisateur du projet |
| `app/(public)/connexion.tsx` (L1-04) : le déplacement réel du focus clavier vers le mot de passe après un échec n'est vérifié par aucun test | `Champ` transmet désormais une ref (`React.forwardRef`, ajouté à P1.9 pour cet usage précis) et `connexion.tsx` appelle bien `refMotDePasse.current?.focus()` après un échec d'identifiants (docs/ecrans/L1-04-connexion.md, États : "le focus va au mot de passe") — le CODE existe et fait ce qu'il doit, mais react-native-testing-library ne simule aucun focus clavier réel (aucun pont natif sous Jest) : un test qui vérifierait ce déplacement ne prouverait rien de plus que "la fonction a été appelée", pas que l'appareil obéit. Même famille de limite que le critère 7 de L1-02 juste au-dessus, jamais un oubli de test. | Sur appareil physique, avec un lecteur d'écran actif — avant publication |
| `app/(public)/nouveau-mot-de-passe.tsx` (L1-04) jamais essayé bout en bout avec un vrai courriel de réinitialisation | Même limite que L1-03 (voir l'entrée `etablirSessionDepuisLien` plus haut, désormais partagée par les deux liens) : `bonjour@myfavcoach.fr` n'a pas de SMTP configuré, donc aucun vrai courriel de réinitialisation n'a jamais été reçu ni cliqué. Le décodage de l'URL, l'échange PKCE, le remplacement du mot de passe et la révocation des autres sessions sont chacun prouvés séparément (tests d'écran avec un faux port ; `src/test/rls.banc.ts` contre la vraie base pour la révocation) mais jamais enchaînés depuis un vrai lien reçu par courriel sur un vrai appareil. | Quand un fournisseur SMTP sera choisi (voir l'entrée sur l'expéditeur du courriel) — puis un essai manuel complet, comme prévu pour L1-03 |
| `app/(public)/mot-de-passe-oublie.tsx` (L1-04) : aucune action après l'envoi du lien | La fiche décrit "un champ, une action" et une réponse constante à l'écran — rien après. Assumé, pas oublié : aucun bouton "retour" ou "renvoyer" inventé ici, faute d'être décrit. L'utilisateur revient par le geste système (glissement iOS, bouton matériel Android), déjà disponible sans code supplémentaire. | À confirmer si un futur lot révise cette fiche avec une action explicite |
| `src/services/auth/supabase.ts`, `changerEmail` — `updateUser({ email })` sans `emailRedirectTo`, jamais corrigé | Trouvé en balayant tous les appels du SDK à P1.9 (le même trou qu'`inscrire`/`renvoyerVerification`, déjà corrigés) : `updateUser({ email })` envoie lui aussi un courriel avec un lien de confirmation (documenté dans `node_modules/@supabase/auth-js/dist/main/GoTrueClient.d.ts` : "email updates sends a confirmation link to both the user's current and new email"), donc a besoin du même `emailRedirectTo`. Pas corrigé, à la différence des trois autres : aucune route de destination n'existe pour confirmer un changement d'adresse — ni écran (aucun au jalon 1), ni entrée dans `additional_redirect_urls`. Inventer une valeur maintenant pointerait vers un lien mort. `changerEmail` n'est appelée par aucun écran à ce jour — mort de toute façon, mais un mort qui enverrait un vrai courriel cassé le jour où quelqu'un le réveille sans y penser. | Quand un écran de changement d'adresse sera construit : ajouter la constante de lien profond, la déclarer dans `supabase/config.toml`, et faire passer l'appel par une fonction d'enveloppe dédiée — même schéma que `envoyerCourrielInscription`/`renvoyerCourrielVerification`/`envoyerCourrielReinitialisation` (`src/services/auth/supabase.ts`), couverte par `src/test/redirection-courriels-obligatoire.test.ts` |
| `src/services/donnees/supabase.ts` (`portDonneesSupabase`) jamais essayé bout en bout sur un appareil réel | Prouvé à trois niveaux distincts, jamais enchaînés depuis une vraie app en train de tourner : la composition des trois appels (`supabase.test.ts`, avec le client mocké), le fournisseur React qui les orchestre (`fournisseur-donnees.test.tsx`, avec un faux port), et le mécanisme serveur lui-même — `profil_actif_courant()` — contre la vraie base (`src/test/rls.banc.ts`, P1.10). Ce qui manque : lancer l'app réelle, se connecter avec un vrai compte ayant un vrai profil (client ou coach), et observer `app/index.tsx` choisir la bonne destination. Aucun profil réel n'existe encore à ce lot (P1.11 les crée) : cet essai n'est de toute façon pas possible avant. | Après P1.11 (premier profil client créable pour de vrai), un essai manuel sur appareil |
| `VERSION_CGU_ACCEPTEE` (`src/services/auth/supabase.ts`) est une date de code, pas la version d'un vrai texte juridique | Trouvé en préparant P1.11 (en vérifiant que le consentement santé de l'onboarding suit la même règle de version que docs/domaine.md §3.12 impose déjà aux CGU) : `envoyerCourrielInscription` n'envoyait NI `date_naissance` (envoyé en camelCase `dateNaissance`, jamais lu par le déclencheur qui attend le snake_case) NI `cgu_version_acceptee` — l'inscription réelle échouait donc TOUJOURS côté serveur (500 "Database error saving new user"), jamais vu en test faute d'un test contre le vrai serveur (`supabase.test.ts` était mocké, `src/test/rls.banc.ts` crée ses comptes via l'API admin avec ses propres métadonnées, en contournant `inscrire()`). Prouvé rouge (API admin, métadonnées `{ dateNaissance }` camelCase → refusé par le déclencheur) puis vert (métadonnées `{ date_naissance, cgu_version_acceptee }` → compte créé) contre le vrai projet, comptes nettoyés. Les DEUX clés sont corrigées ici. Ce qui RESTE une dette : `VERSION_CGU_ACCEPTEE = '2026-09-04'` date le mécanisme actuel (la phrase implicite d'`app/(public)/inscription.tsx`, "En créant ton compte, tu acceptes les CGU..."), pas un texte CGU réellement rédigé — aucun n'existe encore (lot L11, entrée `app/(public)/index.tsx` plus haut). | Lot L11 : à la rédaction du vrai texte CGU, remplacer `VERSION_CGU_ACCEPTEE` par la vraie version datée du texte publié, jamais la bumper avant |
| `app/(onboarding)/1-identite.tsx` (P1.11, étape 1/4) ET `app/(compte)/informations.tsx` (P1.13b, « Mes informations », côtés client ET coach) : ni photo ni commune | Décision explicite avant construction, confirmée à P1.13b (docs/ecrans/L1-05 et L1-09 listent les deux comme des champs) : aucune infrastructure de stockage (Supabase Storage, aucun bucket créé à ce jour) ni de référentiel INSEE des communes (aucun jeu de données, aucune table) n'existe dans ce dépôt. Les colonnes `profils_client.photo_url` / `commune_insee` et `profils_coach.photo_url` / `commune_base_insee` existent déjà (0001_creer_identite.sql, accordées en UPDATE) mais restent NULL — aucun écran ne les écrit, ce n'est pas un oubli. Un champ qui ne peut rien enregistrer est pire qu'un champ absent : les deux sont retirés de l'écran, pas grisés. **Deux blocages distincts, deux solutions distinctes, ils ne se lèveront pas ensemble.** | **Photo** : quand un bucket Supabase Storage sera créé (bucket + politiques + chemin d'upload ; `EmplacementImage` sait déjà afficher un repli initiales, `emplacement-image.tsx` a déjà la prop `source`). **Commune** : quand un référentiel INSEE (table importée ou service) sera choisi — la fiche L1-05 prévoit une complétion réseau à 300 ms, pas conçue au-delà de cette phrase |
| `app/(compte)/informations.tsx` (P1.13b) : discipline du coach en **lecture seule** dans « Mes informations » | La liste figée `disciplinesCoach` a été **définie à P1.14** (`src/fixtures/demonstration.ts`, 7 entrées, arbitrée avec le porteur du projet) et le champ y est éditable à l'**activation** (`app/(onboarding)/devenir-coach.tsx`). Ce qui RESTE : le rendre éditable dans « Mes informations », en menu à choix unique sur `disciplinesCoach`. `profils_coach.discipline` est déjà accordée en UPDATE ; rien ne bloque techniquement, ce n'est simplement pas dans le périmètre d'un prompt à ce jour. | À reprendre avec la publication du profil coach (L2), ou plus tôt si le besoin se présente : brancher `enregistrerInformations` sur la colonne `discipline` côté coach + un sélecteur sur `disciplinesCoach` |
| Service de courriel intégré Supabase (dev) : ~2 courriels/heure, plafond dur du plan gratuit — une inscription, un reset ou un magic link de trop et rien n'arrive pendant l'heure | Diagnostiqué après une session perdue (le déclencheur réel était l'anti-énumération, pas le quota — mais le quota EST une limite réelle pour le dev). Le banc RLS ne le consomme pas (comptes créés `email_confirm: true` via l'API admin, aucun envoi ; le cas changement d'adresse passe par `generate_link`, aucun envoi non plus). Contournement immédiat sans réglage : `POST /auth/v1/admin/generate_link` (`type: signup`) rend le lien directement, à ouvrir dans le simulateur. | **Porte de sortie du lot L1** (`docs/prompts/L1.md` §Porte de sortie), avec le reste de l'outillage : configurer un SMTP personnalisé sur le projet de dev (Dashboard → Auth → SMTP Settings) — fournisseur au choix parmi Resend (100/j gratuit), Brevo (300/j), Mailgun, AWS SES, ou un Gmail via *app password*. C'est le même réglage qu'il faudra pour la prod |
| Garde de sortie (`useGardeSortie`, `src/fonctionnalites/navigation/garde-sortie.tsx`, P1.13b) : ne couvre QUE les sorties « retour » | `beforeRemove` (React Navigation, via `useNavigation()`) se déclenche sur chevron, geste iOS, retour matériel Android et `router.back()` — jamais quand le groupe `(compte)` est démonté par le haut : bascule d'espace, lien profond, notification, déconnexion, tous des `router.replace` au niveau `_layout` ou `FeuilleBascule`. Pour `app/(compte)/informations.tsx` c'est sans conséquence : cet écran ne rend ni bascule d'espace, ni avatar, ni gestion de lien profond — ses seules sorties réelles SONT des « retour ». Le résidu (un lien profond ou une notification reçus pendant l'édition) est au niveau application, frappe tout écran pareil, et la seule vraie parade serait un état « sale » global que chaque acteur de navigation consulterait. | À revoir quand L5 (édition d'offre) ou L8 (rédaction de message) amènent un formulaire éditable AVEC de la navigation interne (ces écrans-là ont leur propre pile) : si le besoin se confirme, un registre « modifications non enregistrées » consultable par `FeuilleBascule` et par le traitement des liens profonds de `app/_layout.tsx` |
| `app/(onboarding)/*.tsx` (P1.11) : aucune garde n'empêche de revisiter un écran d'onboarding après la fin réelle du parcours | `determinerDestination` (garde.ts) ne redirige jamais VERS `(onboarding)/*` une fois `onboarding_etape > 4` — mais rien n'empêche non plus un utilisateur déjà à l'étape 4 d'utiliser le geste retour du téléphone pour revenir sur une étape antérieure APRÈS avoir pressé "Découvrir des coachs" (qui a déjà appelé `terminerOnboarding()`). Conséquence : au pire un formulaire obsolète brièvement revisité, jamais un problème de données (RLS et le déclencheur de consentement continuent de s'appliquer normalement à toute écriture, quel que soit l'écran qui l'a déclenchée) ni de sécurité. Aucune des sept règles de P1.10 ne couvre ce cas (elles ne gouvernent que la redirection au démarrage/depuis (public), jamais la navigation interne). | Pas urgent — à revoir si un vrai utilisateur rapporte l'avoir vécu, ou si un futur lot construit une garde de route générique pour `(onboarding)` |
| `src/services/auth/supabase.ts`, `envoyerCourrielInscription` (et tout futur appel qui écrit dans `user_metadata`) : **angle mort permanent**, pas un travail en retard | GoTrue stocke `options.data` (`user_metadata`) en JSONB libre : aucune validation de clé n'existe côté serveur au moment de l'écriture — une clé mal nommée (`dateNaissance` au lieu de `date_naissance`, exactement le bug de P1.11) est acceptée sans un mot, et ne se révèle QUE plus tard, indirectement, quand un déclencheur qui la lit avec `->>'bon_nom'` la trouve absente. Aucun mécanisme de ce dépôt — ni de PostgREST en général — ne peut détecter ça automatiquement, contrairement à une colonne de table réelle (typée, qui refuse une écriture sur un nom inconnu). **La seule parade réelle** : un test qui appelle `portAuthSupabase.inscrire()` par le vrai chemin du port (pas une reconstruction indépendante de sa charge utile, qui retomberait dans le même travers que le test mocké — voir la 5ᵉ entrée du tableau des faux verts, `docs/prompts/L1.md`) et vérifie que la ligne `comptes` qui en résulte porte bien `date_naissance` et `cgu_version_acceptee`. **Vérifié infaisable tel quel dans `src/test/rls.banc.ts` aujourd'hui**, pas supposé : importer `portAuthSupabase` dans ce fichier échoue au chargement du module — `jest.rls.config.js` est délibérément SANS le préréglage `jest-expo` (son mock de `NativeModules` remplace `global.fetch` par une réponse vide, ce qui rendrait le banc aveugle à un vrai échec réseau, voir l'en-tête du fichier) ; sans lui, `react-native-url-polyfill/auto` (importé par `src/services/supabase/client.ts`) échoue à l'analyse (`SyntaxError: Cannot use import statement outside a module`), et le réparer demanderait `@babel/preset-env`, une dépendance absente du dépôt (CLAUDE.md §4 : aucune dépendance sans demander) — sans même garantie que ce soit la dernière marche (`expo-secure-store`, module natif, viendrait probablement ensuite). Le conflit est architectural, pas une case de config oubliée : le banc a besoin du vrai réseau de Node, le port réel a besoin du runtime React Native. | Pas de lot cible : à retenter seulement si `jest.rls.config.js` peut un jour transformer les paquets React Native SANS charger le mock `NativeModules` de `jest-expo` (un préréglage qui isolerait les deux), ou si `envoyerCourrielInscription` est un jour extrait dans un module qui n'importe plus `src/services/supabase/client.ts` directement |
| `EtatProfils.attentesCoach` (`src/services/donnees/port.ts`) rend toujours 0, codé en dur dans `portDonneesSupabase.lireEtatProfils` — aucun calcul serveur | docs/ecrans/L1-06-bascule-espace.md (P1.12) : "il n'y a ni message ni demande" à ce lot — ni messagerie (L8) ni demandes ne sont des tables qui existent. Une fonction SQL qui rendrait 0 sans rien calculer aurait été un faux point d'ancrage (elle aurait l'air d'une source de vérité qui n'en est pas une) : la valeur reste donc dans le code applicatif, pas dans une migration. Le compteur n'étant de toute façon jamais rendu à 0 (`docs/ecrans/L1-06-bascule-espace.md`, "Compteur à zéro"), rien n'est visible avant que ce ne soit branché. | L8 : calculer réellement `messagesNonLus` + `demandesEnAttente` (docs/api.md §3) et les exposer par une vraie requête, pas une constante |
| Zones sûres : `npm run test:a11y` ne vérifie qu'un seul point, la valeur numérique du retrait haut sur une liste blanche d'écrans — **angle mort permanent pour tout le reste**, de la même famille que le rendu web (`CLAUDE.md` §6) | Trouvé à P1.12 : le bloc encre de `app/(client)/moi.tsx` s'affichait sous la barre d'état sur simulateur (« Devenir coach » derrière l'heure et l'encoche) et rien ne l'a signalé. `react-test-renderer` (donc `npm test`, `test:a11y`, les tests d'écran) ne fait **aucune mise en page** : ni Yoga, ni flexbox résolu, ni coordonnées — un écran qui applique l'inset et un écran qui l'ignore rendent le même arbre, à un nombre près dans un objet de style. Les métriques fixes de `METRIQUES_ZONES_SURES` (`src/test/accessibilite.test.tsx` : haut 59, bas 34) ne sont là que pour que `useSafeAreaInsets()` ne lève pas ; elles ne sont comparées à la position de rien. Parade partielle posée à P1.12 : `verifierOffsetHautZoneSure` + la liste `ECRANS_CHROME_HAUT` (`accueil`, `pilotage`, `moi`) exigent que le premier contenu de ces écrans soit retraité d'au moins l'inset haut — ce qui attrape ce défaut précis, y compris si P1.13 réécrit `moi` sans l'inset. Ce que ça n'attrape PAS : la géométrie réelle (contenu réellement tronqué ou non, à quel pixel), l'inset **bas**, la barre gestuelle Android (jusqu'à 48 dp, > les 34 simulés), la troncature propre à l'encoche, et tout écran hors liste. `app/(onboarding)/devenir-coach.tsx` (écran réel depuis P1.14) applique l'inset haut via `EnteteOnboarding`, comme les quatre écrans d'onboarding client — hors `ECRANS_CHROME_HAUT` par la même convention (ce contrôle ne connaît pas la structure de `EnteteOnboarding`). | Avant publication, sur appareil physique (iOS + Android). Ajouter un écran à `ECRANS_CHROME_HAUT` quand il gagne son propre chrome haut. Une vraie vérification géométrique resterait à envisager le jour où un simulateur serait disponible (voir la même limite en `CLAUDE.md` §6 pour le rendu web) |
