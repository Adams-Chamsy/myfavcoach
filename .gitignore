# dependances
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# natif
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
/ios
/android

# Metro
.metro-health-check*

# journaux
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# secrets locaux
.env
.env.local
.env*.local

# secrets du banc RLS (src/test/rls.banc.ts) : nom délibérément hors de la convention .env*
# réservée par Expo — voir le commentaire de scripts/verifier-serveur-dev.mjs et docs/dette.md,
# collision trouvée à P1.8 (.env.test.local faisait planter `expo start --web`).
.secrets-rls.local

# TypeScript
*.tsbuildinfo

# couverture de tests
coverage/

# cache local de la CLI Supabase
supabase/.temp/
